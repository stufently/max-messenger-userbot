"""Пакет тестов.

Файл нужен ради ``from tests.conftest import ...``: без ``__init__.py`` каталог
не является пакетом, и голый ``pytest`` падает на сборке (``python -m pytest``
маскирует проблему, добавляя текущий каталог в ``sys.path``). Заодно conftest
остаётся одним модулем, а не двумя копиями под разными именами.
"""
