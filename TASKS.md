# Рабочий журнал

Только текущие задачи и последние ~7 дней. Выполненное старше выносится в
`docs/worklog-archive/YYYY-MM.md`. История изменений кода — в
[CHANGELOG.md](CHANGELOG.md), она здесь не дублируется.

## IN_PROGRESS

- **Красный прогон на `b151194`**: упали `Тесты (py3.14)` и `Образ и тесты
  внутри него`, оба на шаге `pytest`; матрица 3.11–3.13, линтер, типы, колесо и
  сверки версий зелёные. Локально не воспроизводится: семь прогонов из семи
  зелёные — образ, собранный `--no-cache` (тот же закреплённый набор), запуск
  как в CI, отдельно с `--cpus=1` и `--cpus=2`, плюс свободная установка
  `.[dev]` на `python:3.14-slim`. Логи прогона недоступны без прав
  администратора, поэтому сначала добавлен вывод падений в аннотацию (см.
  CHANGELOG), и причина читается со следующего прогона. Тег `v0.2.0` не
  ставится, пока прогон не зелёный: релиз собирается с проверенного состояния.

## Требует живой Windows

Файлы для проверки лежат в
[последнем релизе](https://github.com/stufently/max-messenger-userbot/releases/latest):
`maxub.exe`, `maxubctl.exe` и `build-env.txt` — запись о среде, которая их
собрала.

Собранный `maxub.exe` проверен под Wine: демон стартует, панель отдаётся,
`maxubctl.exe` находит демон, второй запуск не поднимает вторую копию. Под Wine
принципиально не проверяются:

- открытие браузера системной командой;
- окно с ошибкой, реальный `%LOCALAPPDATA%` и его ACL;
- предупреждение SmartScreen: exe не подписан, при первом запуске оно будет;
- **DPAPI**: сам вызов `crypt32.dll`, привязка конверта к учётной записи,
  поведение после смены пароля и при переносе профиля. Проверена только
  политика выбора источника — она от платформы не зависит.

## Требует живого Linux с сеансом

- **Secret Service**: настоящий GNOME Keyring или KWallet, заблокированная
  связка, отсутствие D-Bus под systemd-юнитом. В контейнере хранилища нет
  вовсе, поэтому проверялась только деградация к файлу.
- **Юнит systemd под живым сеансом**: `packaging/linux/maxub.service` принят
  `systemd-analyze verify` (systemd 257), но запуск через `systemctl --user`,
  работу песочницы (`ProtectSystem=strict` вместе с записью в каталог данных) и
  доступность сессионной шины из-под юнита в контейнере не проверить — там нет
  ни systemd как init, ни сеанса пользователя.

## Требует участия человека

Эти шаги нельзя выполнить из кода — нужны доступы и живой аккаунт.

1. **Сделать пакет в GHCR публичным** — первая отправка образа создаёт пакет
   приватным независимо от того, что репозиторий открыт: в GitHub это два
   независимых объекта. Переключатель — на странице пакета,
   `https://github.com/users/stufently/packages/container/max-userbot/settings`,
   раздел «Danger Zone» → Change visibility. Там же «Manage Actions access»:
   репозиторию нужен доступ `Write`, иначе следующий прогон не обновит образ.
   Страница появляется только после первой публикации.
2. **Spike по транспорту** — `PyMax` против `pyromax` на живом аккаунте MAX:
   авторизация по телефону и по QR, reconnect, пагинация истории, медиа,
   edit/delete, пропуски событий, два аккаунта одновременно. Нужен номер
   телефона и готовность к возможной блокировке аккаунта. Решение принимается
   по результату, а не по числу звёзд.
3. **Заполнить настройки GitHub** — description и topics по
   [docs/seo.md](docs/seo.md). Deploy key прав на настройки репозитория не
   имеет, делается вручную в веб-интерфейсе.
4. **Забронировать `max-userbot` на PyPI** — нужен аккаунт PyPI. Имя свободно
   на 2026-07-29.

## Дальше по коду

5. **Проверка адаптера на живом аккаунте** — см. пункт 2: без неё адаптер
   остаётся написанным по исходникам библиотеки, а не по поведению сервера.
6. **Лимиты по чату и адресату, суточные квоты, «прогрев» свежего аккаунта.**
   Механика ключа `(аккаунт, действие)` это уже позволяет, но пороги без данных
   живого сервера будут выдуманными. Ждёт спайка, см. пункт 2.
7. **Тест транспорта на настоящем websocket-сервере.** Сейчас тесты подменяют
   библиотеку транспорта фейком, поэтому остаются зелёными при любой версии
   `websockets` — и обновление до 17.0 пришлось отложить, проверить его нечем
   (причина записана в `packaging/constraints.txt`). Нужен локальный сервер,
   говорящий по протоколу настолько, чтобы адаптер прошёл handshake, получил
   сообщение и разобрал закрытие соединения. Живого аккаунта MAX не требует, в
   отличие от пунктов 2 и 5.
8. **Образ под arm64** — Raspberry Pi и облачные Graviton/Ampere. Сейчас
   публикуется только amd64. Цена: privileged-контейнер с эмуляцией (binfmt) в
   цепочке сборки, замедление установки под QEMU и сторонний образ, который
   придётся закреплять по digest, — то есть ровно то, чего сейчас в публикации
   нет. Оба ревьюера сошлись на том, что это второй заход, а не первый.

Отложено за пределы v1: планировщик, плагины через `entry_points`, SPA, второй
транспортный адаптер, мост в Telegram (первый внешний плагин после стабилизации
ядра).

Не будет сделано: подмена отпечатка клиента (случайные имя приложения и версия
ОС, как у Heroku) — это обход детектирования, а не личная автоматизация;
обоснование в [docs/research.md](docs/research.md).

## Решено

- **2026-07-29** — ниша: платформа автоматизации пользовательских аккаунтов,
  не бот-фреймворк. Основание — в [docs/research.md](docs/research.md).
- **2026-07-29** — имя репозитория `max-messenger-userbot`, PyPI-пакет
  `max-userbot`, CLI `maxub`. Основание — в [docs/seo.md](docs/seo.md).
- **2026-07-29** — аудитория и документация: только русскоязычная.
- **2026-07-29** — стек принят с правками после ревью Codex:
  [docs/stack.md](docs/stack.md). Antigravity в ревью не участвовал — не пройдена
  OAuth-авторизация Google.
- **2026-07-29** — каркас v1 реализован и проверен в Docker; замечания ревью
  учтены, перечень исправлений — в [CHANGELOG.md](CHANGELOG.md).
- **2026-07-29** — веб-панель отдаёт тот же демон, а не отдельный сервер:
  аутентификация браузера сведена к обмену токена на сессионную cookie, чтобы
  токен не оседал в `localStorage`.
- **2026-07-29** — сверка с опенсорсными аналогами проведена, выводы разнесены:
  что взято и что отвергнуто — в [docs/research.md](docs/research.md), что
  изменилось в коде — в [CHANGELOG.md](CHANGELOG.md), что осталось — в списке
  выше. Ревью Codex и Antigravity закрыто: обе находки (завышенная оценка
  ожидания и окно между проверкой и захватом разрешения) исправлены.
- **2026-07-29** — красный `windows-build` разобран без доступа к логам прогонов
  (публичный API отдаёт только шаг и код выхода): отказ воспроизведён локально
  под Wine и исправлен, что именно — в [CHANGELOG.md](CHANGELOG.md). Открытый PR
  Dependabot на `pydantic-core` закроется сам после нового обхода — правило
  теперь в `.github/dependabot.yml`.
- **2026-07-29** — права доступа к API разделены на области, а контракт точки
  расширения зафиксирован до v1 (бывшие пункты 6 и 7). Что именно изменилось —
  в [CHANGELOG.md](CHANGELOG.md). Ревью Codex и Antigravity закрыто: эскалация
  через веб-сессию и одноразовый код входа, гарантия диспетчера при ошибке
  обработчика и уборка журнала мимо курсоров — исправлено.
- **2026-07-30** — CPython 3.14.6 принят и для рабочего образа, и для релизного
  exe: обновление проверено локально целиком (закреплённый набор встал колёсами,
  тесты и проверки зелёные, exe пересобран под Wine и запускается), нативная
  сборка на `windows-2025` остаётся гейтом в CI, а целевая платформа
  exe — Windows 11, обратная совместимость со старыми Windows не требуется.
  Debian не тронут: уже trixie на свежем digest. Что именно изменилось — в
  [CHANGELOG.md](CHANGELOG.md). Ревьюеры разошлись только в одном: Antigravity
  предлагал оставить exe на 3.13.14 «из осторожности», Codex — двигать, если
  нативная сборка зелёная. Принят второй довод: релизный артефакт выпускает
  нативный `windows-2025`, и он же остаётся гейтом, а Wine — локальная проверка.
  `websockets` до 17.0 не поднят, пока его нечем проверить — задача заведена
  пунктом 6 выше.
- **2026-07-30** — запуск на Linux: без своего бинарника, через `pipx` и
  публикуемый образ. Одиночный ELF отвергнут — база `python:3.14-slim` это
  trixie с glibc 2.41, собранный там файл не пошёл бы на Ubuntu 24.04 (2.39) и
  Debian 12 (2.36), понадобился бы `manylinux_2_28` и третья матрица сборки, а
  колесо `py3-none-any` и образ закрывают ту же потребность. Оба ревьюера с этим
  согласились. Публикуется пока только amd64 (пункт 8 выше). Что именно
  изменилось — в [CHANGELOG.md](CHANGELOG.md). Ревью Codex и Antigravity
  закрыто: дефолт каталога был дублирован в `Settings` и `ClientSettings`
  (демон создал бы токен в одном месте, а CLI искал в другом), smoke стоял после
  публикации вместо «до», `ReadWritePaths` в юните противоречил переменному
  XDG-пути, а непривилегированный пользователь образа конфликтовал с прошитым
  `user:` в compose — исправлено всё. Против обоих ревью упрощено одно: раз arm64
  отложен, buildx с эмуляцией не нужен вовсе — обычный `docker build` плюс
  `docker push`, без сторонних actions в цепочке поставки.
- **2026-07-30** — выпущен первый релиз `v0.1.0`: колесо, sdist и два exe
  прикреплены к нему автоматически, все четыре работы прогона зелёные. До этого
  тегов в репозитории не было ни одного, поэтому релиза не существовало, а
  собранный exe лежал только артефактом прогона — то есть скачивался лишь
  из-под входа в GitHub. Публикация делает файлы доступными по прямой ссылке.
- **2026-07-30** — среда релизной сборки: полное закрепление системной части на
  раннере GitHub недостижимо (образ обновляется примерно раз в неделю, закрепить
  состояние можно только на собственном раннере — цена не стоит), поэтому
  закреплено поколение ОС, а остальное записывается в артефакт. Что именно — в
  [CHANGELOG.md](CHANGELOG.md). Ревью Codex и Antigravity закрыто: проверка
  запуска exe теперь смотрит на вывод, а не только на код возврата, и ограничена
  по времени.
- **2026-07-29** — второе ревью Codex закрыто: гарантии «без потерь и дублей»
  держатся на разделении `claimed`/`sending` и на трёхзначном контракте сверки.
  Antigravity в обоих ревью не участвовал — не проходит OAuth-авторизация.
