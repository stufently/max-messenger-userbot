# Рабочий журнал

Только текущие задачи и последние ~7 дней. Выполненное старше выносится в
`docs/worklog-archive/YYYY-MM.md`. История изменений кода — в
[CHANGELOG.md](CHANGELOG.md), она здесь не дублируется.

## IN_PROGRESS

- **Ревью стека заблокировано.** Каталог сценариев собран
  ([docs/features.md](docs/features.md)), стек предложен
  ([docs/stack.md](docs/stack.md)), но оба ревьюера недоступны на 2026-07-29:
  Codex отдаёт `401 Unauthorized` на `api.openai.com` (две попытки),
  Antigravity требует интерактивный OAuth-логин Google. Требуется повторная
  авторизация обоих. До этого стек в код не переносится.

## Ближайшие шаги

1. **Заполнить настройки GitHub** — description и topics по
   [docs/seo.md](docs/seo.md). Делается вручную в веб-интерфейсе.
2. **Забронировать имя `max-userbot` на PyPI** — свободно на 2026-07-29,
   занять до релиза.
3. **Восстановить доступ к Codex и Antigravity** — без них не проходят ревью
   ни стек, ни выбор транспорта.
4. **Выбрать библиотеку транспорта** — сравнить `MaxApiTeam/PyMax` (231★,
   активна) и `rast-games/pyromax` (27★, aiogram-подобный API) по покрытию
   методов, стабильности и лицензии. Прогнать выбор через Codex и Antigravity.
5. **Определить лицензию проекта** — до первого коммита с кодом.
6. **Спроектировать слой абстракции над транспортом** — интерфейс, который
   переживёт смену внутреннего API MAX.
7. **Схема хранения сессий** — формат, шифрование, права доступа, ротация.
8. **Каркас репозитория** — Dockerfile, docker-compose, структура пакета,
   линтеры, CI.

## Решено

- **2026-07-29** — ниша: платформа автоматизации пользовательских аккаунтов,
  не бот-фреймворк. Основание — в [docs/research.md](docs/research.md).
- **2026-07-29** — имя репозитория `max-messenger-userbot`, PyPI-пакет
  `max-userbot`, CLI `maxub`. Основание — в [docs/seo.md](docs/seo.md).
- **2026-07-29** — аудитория и документация: только русскоязычная.
