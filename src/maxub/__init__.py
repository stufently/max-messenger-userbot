"""MAX Userbot — автоматизация пользовательских аккаунтов мессенджера MAX."""

# Единственное место в коде, где записан номер версии. Второй экземпляр уже
# разошёлся с первым: при выпуске 0.2.0 в pyproject значение здесь и в заголовке
# FastAPI осталось на 0.1.0, и нашлось это только на ревью. Теперь номер здесь
# один, API берёт его отсюда, а совпадение с pyproject сторожит тест
# (`tests/test_version.py`) — из установленного пакета версия читается
# метаданными, и подгонять её вручную больше негде.
#
# Читать метаданные прямо здесь нельзя: в собранном PyInstaller-ом exe их нет,
# если явно не попросить их положить, — и `maxub.exe` падал бы на импорте.
__version__ = "0.2.0"
